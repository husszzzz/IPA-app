name: Build MoonManager
on: [push, pull_request]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: brew install ldid

      - name: Setup Theos
        run: |
          # تنظيف أي بناء سابق لضمان عدم حدوث خطأ 128
          rm -rf ~/theos
          git clone --recursive https://github.com/theos/theos.git ~/theos
          echo "THEOS=~/theos" >> $GITHUB_ENV

      - name: Setup SDK
        run: |
          # تنظيف الـ SDKs القديمة وتحميل الجديدة
          rm -rf ~/theos/sdks
          git clone https://github.com/theos/sdks.git ~/theos/sdks

      - name: Build Application
        run: |
          export THEOS=~/theos
          make clean all

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MoonManager-App
          # المسار المحدث لضمان العثور على الملف ورفعه في الـ Artifacts
          path: "**/obj/*.app"
